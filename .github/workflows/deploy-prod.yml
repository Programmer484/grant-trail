name: Deploy to Production

# Gated production deploy. Manual trigger only; the `production` GitHub
# Environment's required-reviewer protection provides the approval gate.
# Delegates the full deploy pipeline to the reusable deploy.yml.
on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: production
    secrets: inherit
